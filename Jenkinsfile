pipeline {
   agent any

   stages {
      stage('GitCheckout') {
         steps {
            echo 'Checking Out code'
         }
      }
       stage('Packaging') {
         steps {
            echo 'Using Maven'
         }
      }
       stage('Transfer') {
         steps {
            echo 'Moving'
         }
      }
	  stage('Temp') {
         steps {
            echo 'Temp'
         }
      }
   }
}
